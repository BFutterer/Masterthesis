/* website functionality
 * This file contains all function which deals with file handling:
 *  - Export of the drawn isochrones together with the starting position to a GeoJSON-file
 *  - Import of one or several GeoJSON-file(s) and draw the content of them into the map
 *  - Merge of several GeoJSON-files into a single one
 *
 */


 /*to find error*/	//function alertMessage(){alert("minutes: " + minutes + " traveltime: " + traveltime + " parse: " + stringToInt)}
 //debugger;		//not in a function
 //JSON.stringify und JSON.parse machen genau das Gegenteil

 //"var variableXYZ" in einer Funktion = lokale Variable		nur "variableXYZ" in einer Funktion = globale Variable
